import React from "react";
import { useNavigate } from "react-router-dom";


function Feed() {
  const navigate = useNavigate();

  const displayFeed = () => { // shows posts in chronological order
    
  };

  const handleLike = () => { // adds a like to a post

  };

  return (
    <div>
      <h1>Feed</h1>
    </div>
  );
}

export default Feed;